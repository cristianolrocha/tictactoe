import React, { PureComponent } from 'react';
import { Game } from '../screens/Game';

export class EnableCameraContainer extends PureComponent {
  render() {
    return <Game />;
  }
}

export default EnableCameraContainer;
